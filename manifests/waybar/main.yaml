actions:
  - procedure: aur.install
    info: Installing waybar
    packages: ["waybar"]

  - procedure: os.symlink
    info: Creating waybar config symlink
    source: "./files/waybar"
    target: "{{ user.config }}/waybar"
